import { AnimateCardOnScroll } from './animate-card-on-scroll';

describe('AnimateCardOnScroll', () => {
  it('should create an instance', () => {
    const directive = new AnimateCardOnScroll();
    expect(directive).toBeTruthy();
  });
});
